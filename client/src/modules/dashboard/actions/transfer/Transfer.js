import Button from '../../../common/button/Button';

import './Transfer.scss';

const Transfer = () => {
  return <Button styling={'action-transfer'} title='Transfer' />;
};

export default Transfer;
